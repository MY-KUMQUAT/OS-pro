#include "OS_pro.h"
/*
void enter(char directoryname[])
{
    //detect if or not exist;
    for(int i=0;i<num_OF_datablock;i++)
    {
        if( (strcmp(directoryname,d_or_f[i].dir_directoryname)!=0) )
        {
            cout<<"The directory "<<directoryname<<"does not exist"<<endl;
        }
        else
        {
            d_or_f[inode[d_or_f[i].df_inum].inode_inum].dir_directoryname=directoryname[];
            break;
        }
    }

    //keep current path (directory name)
    //modify current directory

    last_directory=current_directory;
    current_directory=directoryname;

    //enter, according name to find i_node ,convert
}
*/
